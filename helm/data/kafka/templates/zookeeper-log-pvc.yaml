kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: datalogdir-kafka-cp-zookeeper-0
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ index .Values.kafka "cp-zookeeper" "persistence" "dataLogDirSize" }}
  selector:
    matchLabels:
      name: zookeeper-log-0
